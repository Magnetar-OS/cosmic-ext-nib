[package]
name = "cosmic-ext-nib-spell"
description = "Spell checking for a Nib document, against the system's dictionaries"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
authors.workspace = true

[lib]
name = "nib_spell"
path = "src/lib.rs"

[dependencies]
nib-model.workspace = true
unicode-segmentation.workspace = true
thiserror.workspace = true
# Hunspell's format, in Rust, with no C dependency. The dictionaries
# themselves come from the system — see the crate documentation for why.
spellbook = "0.4"

[lints]
workspace = true
