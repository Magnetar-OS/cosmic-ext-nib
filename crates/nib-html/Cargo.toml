[package]
name = "cosmic-ext-nib-html"
description = "HTML in and out of a Nib document"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
authors.workspace = true

[lib]
name = "nib_html"
path = "src/lib.rs"

[dependencies]
nib-model.workspace = true
# The safe subset of CSS. `style=` attributes and the presentational
# attributes that predate them become marks here; what a colour *means* against
# a given background is the view's question, not this crate's.
nib-css.workspace = true
html5ever.workspace = true
markup5ever_rcdom.workspace = true

[lints]
workspace = true
